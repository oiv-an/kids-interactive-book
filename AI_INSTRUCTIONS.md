# 📋 Инструкции для работы AI с проектом

## 🎯 Основные принципы

### 🌍 ВАЖНО: i18n-first (переводы всегда и сразу)
При добавлении **любых модулей, плагинов, инструментов, UI-компонентов и вообще любых новых фич**:

- **Никакого RU/EN хардкода в UI.** Любые новые пользовательские строки (заголовки, кнопки, ошибки, подсказки, плейсхолдеры, toast’ы) должны идти через i18n.
- Переводы добавлять в [`src/i18n/resources.ts`](src/i18n/resources.ts:1) в формате **`RU|EN`** (двухъязычно).
- Использовать `t('namespace.key')` / `<Trans />`. Namespace выбирать по фиче (`images.*`, `gallery.*`, `kids.*`, `voice.*` и т.д.).
- Если интеграция/плагин приносит UI-строки — оборачивать/переопределять их так, чтобы они также использовали i18n.

### ⚠️ ВАЖНО: Процесс работы над новым заданием

**Перед началом реализации ОБЯЗАТЕЛЬНО:**

1. **Проанализировать задачу** - изучить текущий код и понять контекст
2. **Построить архитектуру** - спланировать структуру решения
3. **Описать реализацию:**
   - Как это будет выглядеть (UI/UX)
   - Как это будет работать (логика)
   - Какие файлы будут затронуты
   - Какие компоненты будут созданы/изменены
4. **Предложить варианты** - 1-2 варианта реализации с плюсами и минусами
5. **Дождаться согласования** - получить одобрение перед началом кодирования

**🚫 НЕЛЬЗЯ сразу приступать к написанию кода без согласования плана!**

**✅ ИСКЛЮЧЕНИЕ:** Если пользователь дает прямое указание вида "исправь вот это", "поменяй здесь", "добавь сюда" - можно сразу приступать к исправлению без предварительного планирования и согласования.

---

### 1. Валидация кода
После ЛЮБЫХ изменений в коде обязательно запускать проверку:
```bash
npx tsc -b && npm run lint
```
- Исправляй все ошибки линтера
- Следи за предупреждениями TypeScript

### 2. Процесс разработки
- **npm run dev** - УЖЕ ЗАПУЩЕН, НЕ ПЕРЕЗАПУСКАТЬ
- **Браузер НЕ открывать** - пользователь сам проверяет изменения
- **Build НЕ запускать** - пользователь делает сам

### 3. Завершение работы

#### ⚠️ ВАЖНО: Коммит и push делать ТОЛЬКО по команде
Пользователь сам скажет: "Сделай коммит" или "Закоммить изменения"

#### Когда получена команда на коммит:
1. Проверить статус изменений:
   ```bash
   git status
   ```

2. Добавить все изменения:
   ```bash
   git add .
   ```

3. Создать коммит с ПОДРОБНЫМ описанием (на английском языке):
   ```bash
   git commit -m "feat/fix/refactor: Brief description

   Detailed changes:
   - What was changed
   - Which files were affected
   - What functionality was added/fixed
   - Side effects (if any)"
   ```

4. Отправить в репозиторий:
   ```bash
   git push
   ```

## 🔢 Управление версиями

### Файл VERSION.json
В корне проекта находится файл `VERSION.json`, который содержит текущую версию приложения.

### Процесс обновления версии при коммите

**Когда пользователь говорит "сделай коммит":**

1. **Спросить о типе версии:**
   - Предложить варианты: `patch`, `minor`, `major` или ручной ввод
   - По умолчанию рекомендовать `patch` для мелких изменений

2. **Типы версионирования (Semantic Versioning):**
   - **PATCH** (1.0.X → 1.0.X+1) - исправления багов, мелкие правки, косметические изменения
   - **MINOR** (1.X.0 → 1.X+1.0) - новые функции, добавление возможностей (обратно совместимо)
   - **MAJOR** (X.0.0 → X+1.0.0) - большие изменения, breaking changes, изменение архитектуры

3. **Обновить VERSION.json:**
   ```json
   {
     "version": "1.0.2",
     "updated": "2026-01-02T23:40:00.000Z"
   }
   ```

4. **Формат сообщения коммита с версией:**
   ```bash
   git commit -m "feat: Brief description [v1.0.2]
   
   Detailed changes:
   - What was changed
   - Which files were affected
   - What functionality was added/fixed"
   ```

5. **Включить VERSION.json в коммит:**
   ```bash
   git add VERSION.json
   git add .
   git commit -m "..."
   git push

### Ведение CHANGELOG.md

При КАЖДОМ коммите необходимо обновлять файл `CHANGELOG.md` в корне проекта.

**Правила ведения:**
1. **Двуязычность:** Описание изменений должно быть и на русском (RU), и на английском (EN).
2. **Структура:**
  ```markdown
  ## [Версия] - ГГГГ-ММ-ДД
  ### RU
  - **Добавлено**: ...
  - **Исправлено**: ...
  - **Изменено**: ...
  ### EN
  - **Added**: ...
  - **Fixed**: ...
  - **Changed**: ...
  ```
3. **Синхронизация:** Версия в `CHANGELOG.md` должна строго соответствовать версии в `VERSION.json`.
4. **Детализация:** Писать подробно, что именно было сделано, какие файлы затронуты и какая логика изменилась.

### Примеры решений по версии:

- Исправлен баг в галерее изображений → **patch** (1.0.1 → 1.0.2)
- Добавлена новая функция для детей → **minor** (1.0.2 → 1.1.0)
- Полностью изменен дизайн приложения → **major** (1.1.0 → 2.0.0)
- Обновлены стили кнопок → **patch** (2.0.0 → 2.0.1)

### Отображение версии

Версия автоматически отображается в футерах:
- Внутренний футер приложения: `© 2026 Game Voice By IVOL v1.0.1`
- Landing футер: `© 2026 Game Voice. Сделано с 💚 для веселых игр с голосом. • v1.0.1`

## 📝 Формат коммитов

Используй conventional commits:
- `feat:` - новая функциональность
- `fix:` - исправление багов
- `refactor:` - рефакторинг кода
- `style:` - форматирование, отступы
- `docs:` - изменения в документации
- `chore:` - обновление зависимостей, конфиги

## 🚫 Что НЕ делать

1. **НЕ открывать браузер** через browser_action
2. **НЕ запускать npm run build** 
3. **НЕ делать коммиты** без явной команды от пользователя
4. **НЕ перезапускать npm run dev** - он уже работает
5. **НЕ закрывать** работающие процессы

## ✅ Чек-лист после изменений

- [ ] Код изменен согласно задаче
- [ ] Запущена валидация `npx tsc -b && npm run lint`
- [ ] Исправлены все ошибки линтера
- [ ] TypeScript не показывает ошибок
- [ ] Ожидание команды на коммит от пользователя

## 💡 Дополнительные рекомендации

- Комментируй сложную логику (комментарии в основном для AI)
- Используй типизацию TypeScript везде
- Следи за консистентностью кода с остальным проектом
- При рефакторинге уточняй необходимость обратной совместимости (особенно если меняется backend)

## 🔄 Унификация кода

### Главный принцип: НЕТ ДУБЛИРОВАНИЮ
- Если компонент/функция используется в нескольких местах - создавай единый переиспользуемый компонент
- Модальные окна должны быть едиными для всего приложения
- Общие функции выносить в helpers/utils
- Не копировать код - использовать импорты

### Примеры:
- Одно модальное окно `ImageDetailModal` для всех изображений
- Единые хуки для работы с галереей
- Общие типы в отдельных файлах

## 🎨 Дизайн и стилизация

### Папка React
В корне проекта есть папка `React/` - это референс дизайна:
- Все стили и шаблоны компонентов
- Примеры оформления UI элементов
- Перед созданием нового UI - проверь эту папку

### Правила стилизации:
- Новые компоненты ДОЛЖНЫ вписываться в текущий дизайн
- Использовать существующие классы и стили
- Формы должны быть идентичны по стилю с существующими
- НЕ изобретать новые стили без необходимости

## 🏗️ Архитектурные принципы

### Читаемость и понятность
- Код должен быть самодокументируемым
- Названия переменных и функций - описательные
- Структура должна быть логичной и предсказуемой
- Любой разработчик (или AI) должен легко понять логику

### Организация кода:
```
src/
  views/         - страницы приложения
  components/    - переиспользуемые компоненты
  hooks/         - кастомные React хуки
  helpers/       - вспомогательные функции
  types/         - TypeScript типы
  context/       - React Context провайдеры
```

## 🐛 Отладка и логирование

### Console.log правила:
- ✅ МОЖНО добавлять на этапе тестирования
- ⚠️ ОБЯЗАТЕЛЬНО удалять после подтверждения что всё работает
- 🔴 При ошибках - добавлять информативные console.error

### Процесс:
1. Добавляй console.log для отладки
2. Тестируй функциональность
3. После подтверждения "всё работает" - удаляй все console.log
4. Оставляй только критичные console.error для production

## 📝 Документация и комментарии

### Код для людей и AI:
- Пиши код так, чтобы он был понятен без комментариев
- Добавляй комментарии для сложной логики
- Комментарии могут быть специально для AI-ассистентов

### Примеры комментариев:
```typescript
// AI: Этот хук управляет состоянием галереи изображений
// Использует адаптер для преобразования данных между форматами

// ВАЖНО: Не менять порядок хуков - нарушится логика
```

### Где нужны комментарии:
- Сложная бизнес-логика
- Неочевидные решения
- Места потенциальных ошибок
- Интеграции с внешними API