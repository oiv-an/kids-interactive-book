# Deployment (SSH key) — example
# Copy to .env and fill values

# Enable deploy on build/postbuild (set to 0 to skip)
DEPLOY_ENABLED=0

# SSH connection
DEPLOY_HOST=your.server.ip
DEPLOY_USER=root
DEPLOY_PORT=22
DEPLOY_SSH_KEY_PATH=C:/Users/johan/.ssh/id_rsa

# Remote web root
DEPLOY_REMOTE_PATH=/www/wwwroot/kids.ivol.pro

# Local build folder (CRA uses ./build)
DEPLOY_LOCAL_BUILD_PATH=./build

# Zip name
DEPLOY_ARCHIVE_NAME=deploy.zip
